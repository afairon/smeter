syntax = "proto3";

package service;

import "message/sensor.proto";

// SensorService definition
service SensorService {
    rpc Add(message.Sensor) returns (message.Sensor);
    rpc Get(message.SensorRequest) returns (stream message.Sensor);
}
