syntax = "proto3";

package message;

option go_package = "github.com/afairon/smeter/message";

enum Status {
    ALL = 0;
    ACTIVE = 1;
    INACTIVE = 2;
}

enum Bucket {
    HOUR = 0;
    DAY = 1;
    WEEK = 2;
    MONTH = 3;
    YEAR = 4;
}
